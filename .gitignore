\
# build artifacts
build/
src/quiz/*/build/

# editor
.vscode/

# point cloud sample data (kept out of git to keep the repo lightweight
# for deployment to resource-constrained targets like Jetson Orin Nano;
# see README.md "Sample Data" section)
*.pcd

# generated per-frame detection JSON dumps from tuning/eval runs
# (see experiments/ for the write-ups; scripts/evaluate_detections.py
# regenerates these against any bag/label set)
results/
